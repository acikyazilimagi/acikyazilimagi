---
import MediaPreview from "./media-preview.astro";
const { articles } = Astro.props;
---

<section class="media-preview__list">
  {articles.map((article) => <MediaPreview article={article} />)}
</section>
<style>
  .media-preview__list {
    @apply flex flex-col gap-12;
  }
</style>
